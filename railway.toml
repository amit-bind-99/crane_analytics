# Railway deployment configuration
# https://docs.railway.app/reference/config-as-code

[build]
# Railway auto-detects Dockerfile; this section is optional but explicit
dockerfile = "Dockerfile"

[deploy]
startCommand = "sh -c 'exec streamlit run streamlit_app.py --server.port=${PORT:-8080} --server.address=0.0.0.0 --server.headless=true'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "crane-analytics"
